<h2>タスク一覧</h2>

<% @tasks.each do |task| %>
  <div><%= task.title %></div>
  <%= link_to "詳細",task_path(task.id) %>
  <%= link_to "編集",edit_task_path(task.id) %>
  <%= link_to "削除",task_path(task.id),method: :delete,data:{confirm:"削除しますか？"} %>
<% end %>
<div><%= link_to "タスク新規作成",new_task_path %></div>